{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "echo 'build-skipped'"
  },
  "deploy": {
    "startCommand": "echo 'Deployed via Render only'",
    "healthcheckPath": "/api/health",
    "restartPolicyType": "NEVER"
  }
}